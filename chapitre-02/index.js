// variable (var, à éviter)
var myself = "Quentin";

// Modification de la valeur d'une variable
myself = "Amin";

// variable (let, à préférer plutôt que var)
let age = 28;

// Modification de la valeur
age = 31;

// constantes (pas de réaffectation possible)
const language = "JavaScript";

// Une erreur est levée
// language = "PHP";

// Variable commençant par une lettre minuscule
const paradigm = "functional";

// Variable commençant par une lettre majuscule (peu recommandé)
const List = [];

// Variable commençant par un symbole dollar
const $observable = true;

// Variable commençant par un symbole underscore
const _private = "secret";

// Variable contenant des chiffres (ne peut pas commencer par un chiffre)
const ht2ttc = 20.15;

// Nom de variable composés (camelCase)
const speedOfLight = 300000;
